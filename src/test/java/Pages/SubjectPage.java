package Pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class SubjectPage extends BasePage{
    // Data Member

    // Constructor
    public SubjectPage (WebDriver driver, WebDriverWait wait){
        super(driver, wait);
    }
}
